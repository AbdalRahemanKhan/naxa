services:
  - type: web
    name: naxa-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api/app.py
    envVars:
      - key: NAXA_API_TOKEN
        sync: false